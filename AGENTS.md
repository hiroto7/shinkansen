# Repository instructions

- 挙動や計算方法を変更する前に、`docs/design-principles.md`を読むこと。
- 運賃・料金・交換ポイントの実装根拠には、JRの規則・公式表・公式発表だけを使用すること。乗換案内サイトや予約検索の結果は照合にのみ使用し、ロジックやデータへ取り込まないこと。
- 普通車指定席では、自由席・立席と普通車指定席の比較を維持すること。
- グリーン車とグランクラスでは、指定した設備・行程と同じ所定額を比較すること。普通車との差額や「追加ポイントの価値」を独自に追加しないこと。
- 年版のデータを上書きせず、適用日ごとに分離すること。
- 画面では「はやぶさ・こまち」「グリーン車」「グランクラス」と表記し、内部的な略称を表示しないこと。
- 意味のない装飾、英語の小見出し、説明のない評価指標を追加しないこと。
- 比較軸、入力可能な区間、計算式を変更する場合は、対応するテストと`docs/design-principles.md`を同時に更新すること。
- 実装後は`npm run typecheck`、`npm test`、`npm run build`を実行すること。
